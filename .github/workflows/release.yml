name: Release to Maven Central

on:
  workflow_dispatch:
    inputs:
      releaseVersion:
        description: 'Release version (e.g., 0.1.0)'
        required: true
      nextVersion:
        description: 'Next development version (e.g., 0.1.1-SNAPSHOT)'
        required: true

env:
  MAVEN_USERNAME: ${{ secrets.MAVEN_CENTRAL_USERNAME }}
  MAVEN_PASSWORD: ${{ secrets.MAVEN_CENTRAL_PASSWORD }}
  MAVEN_GPG_PASSPHRASE: ${{ secrets.GPG_PASSPHRASE }}

jobs:
  release:
    name: Build and Release
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up JDK 11
        uses: actions/setup-java@v4
        with:
          java-version: '11'
          distribution: 'temurin'
          cache: 'maven'
          server-id: central
          server-username: MAVEN_USERNAME
          server-password: MAVEN_PASSWORD
          gpg-private-key: ${{ secrets.GPG_PRIVATE_KEY }}
          gpg-passphrase: MAVEN_GPG_PASSPHRASE

      - name: Configure Git
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

      - name: Debug - Check GPG setup
        run: |
          echo "Checking GPG configuration..."
          gpg --version
          gpg --list-keys
          echo "---"
          if [ -z "$MAVEN_GPG_PASSPHRASE" ]; then
            echo "❌ ERROR: MAVEN_GPG_PASSPHRASE is NOT set"
          else
            echo "✅ MAVEN_GPG_PASSPHRASE is set (length: ${#MAVEN_GPG_PASSPHRASE} characters)"
          fi
          echo "---"
          echo "Testing GPG signing with passphrase..."
          echo "test" | gpg --batch --pinentry-mode loopback --passphrase "$MAVEN_GPG_PASSPHRASE" --armor --detach-sign || echo "❌ GPG signing test FAILED"
          echo "GPG test complete"

      - name: Update version to release version
        run: |
          mvn versions:set -DnewVersion=${{ github.event.inputs.releaseVersion }} -DgenerateBackupPoms=false
          git add pom.xml
          git commit -m "Release version ${{ github.event.inputs.releaseVersion }}"

      - name: Build and verify
        run: mvn clean verify -Prelease

      - name: Deploy to Maven Central
        run: mvn deploy -Prelease -DskipTests

      - name: Create Git tag
        run: |
          git tag -a v${{ github.event.inputs.releaseVersion }} -m "Release version ${{ github.event.inputs.releaseVersion }}"
          git push origin v${{ github.event.inputs.releaseVersion }}

      - name: Create GitHub Release
        uses: actions/create-release@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: v${{ github.event.inputs.releaseVersion }}
          release_name: Release ${{ github.event.inputs.releaseVersion }}
          body: |
            Release version ${{ github.event.inputs.releaseVersion }}

            ## Maven Dependency

            ```xml
            <dependency>
              <groupId>io.github.agnistack</groupId>
              <artifactId>custom-central-publishing-maven-plugin</artifactId>
              <version>${{ github.event.inputs.releaseVersion }}</version>
            </dependency>
            ```

            ## Changes

            See [CHANGELOG.md](https://github.com/${{ github.repository }}/blob/main/CHANGELOG.md) for details.
          draft: false
          prerelease: false

      - name: Update to next development version
        run: |
          mvn versions:set -DnewVersion=${{ github.event.inputs.nextVersion }} -DgenerateBackupPoms=false
          git add pom.xml
          git commit -m "Prepare for next development iteration ${{ github.event.inputs.nextVersion }}"
          git push origin main

      - name: Release Summary
        run: |
          echo "✅ Release ${{ github.event.inputs.releaseVersion }} completed successfully!"
          echo "📦 Artifacts deployed to Maven Central"
          echo "🏷️  Git tag created: v${{ github.event.inputs.releaseVersion }}"
          echo "📝 GitHub Release created"
          echo "🔄 Version updated to ${{ github.event.inputs.nextVersion }}"